#ifndef BABLE_LINUX_BASEOPERATION_HPP
#define BABLE_LINUX_BASEOPERATION_HPP

// TODO: if allow to register multiple waiting responses, needs to erase the not used one afterwards
class BaseOperation {

public:
  BaseOperation();
  ~BaseOperation();

private:

};

#endif //BABLE_LINUX_BASEOPERATION_HPP
